# NEXUS UI — Environment Variables
# Copy to .env.local for local development

# URL of the running NEXUS Express backend (default: http://localhost:3001)
NEXUS_API_URL=http://localhost:3001

# For client-side fetches (if calling API directly from browser)
NEXT_PUBLIC_API_URL=http://localhost:3001
